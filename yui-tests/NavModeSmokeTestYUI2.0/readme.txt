1. The nav mode smoke test can be run directly in desktop browser.
2. These are quick smoke tests to varify the JS navmode logic works as expected, they are by no means comprehensive.
3. YUI 2.9.0 (as oppose to YUI 3) is used so that the Logger Console would show up properly on BB 5.0

** Make sure you put replace navmode.js with the one in lib/js folder of the build that you want to test. **